server:
  port: 9090

spring:
  datasource:
    url: jdbc:h2:file:./data/demo;DATABASE_TO_UPPER=false
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update